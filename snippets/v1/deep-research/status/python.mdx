```python Python
from firecrawl import FirecrawlApp

# 初始化客户端
firecrawl = FirecrawlApp(api_key="your_api_key")

# 检查研究状态
status = firecrawl.check_deep_research_status("job_id")

# 打印当前进度
print(f"状态: {status['status']}")
print(f"进度: {status['currentDepth']}/{status['maxDepth']} 迭代")

if status['status'] == 'completed':
    print(f"最终分析: {status['data']['finalAnalysis']}")
    print(f"找到的来源: {len(status['data']['sources'])}")
```
